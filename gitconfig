[user]
  name = Bassam Ismail
  email = skippednote@gmail.com
  signingkey = 6FE151559F58F8AE

[color]
  branch = auto
  diff = auto
  interactive = auto
  status = auto
  ui = auto

[core]
  excludesfile = /home/skippednote/.gitignore_global
  editor = /usr/bin/vi
  fileMode = false

[help]
  autocorrect = 1

[url "git@github.com:"]
	insteadOf = https://github.com/

[commit]
  template = /home/skippednote/.gitmessage
  gpgsign = true

[push]
  branch = current
  default = current

[pull]
  branch = current
  default = simple

[blame]
  date = short

[github]
  user = skippednote

[alias]
  a   = add
  aa  = add --all
  cl  = clone
  c   = commit -v
  cm  = commit -s -m
  cam = commit -s -am
  co  = checkout
  cob = checkout -b
  d   = diff
  pu  = push
  pl  = pull
  pr  = pull --rebase
  s   = status
  sb  = status -sb
  rv  = remote -v
  rhh = reset --hard HEAD
  rhs = reset --soft HEAD
  rhm = reset --mixed HEAD
  lp  = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
  bl  = branch -l
  br  = branch -r
  log = log --show-signature
  cfd = clean -fd
  cf = clean -f
  ignore = update-index --assume-unchanged
  fo = fetch origin
  sl = stash list
  sp = stash pop
  sc = stash clear
  standup = log --since=yesterday.0:00am --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --author='Bassam Ismail'  --abbrev-commit 